<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/player_option_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:paddingHorizontal="30dp"
    android:paddingTop="5dp"
    tools:background="@drawable/truth_or_dare_first_gradiant">

    <EditText
        android:id="@+id/player_field"
        style="@style/TodEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="test"
        android:background="@drawable/background_edit_text_blure_white_60_rounded"
        android:ellipsize="end"
        android:focusable="true"
        android:inputType="text"
        android:paddingStart="20dp"
        android:paddingEnd="50dp"
        android:textColor="@color/black"
        android:textColorHint="@color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:hint="Player 1" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/remove_button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginEnd="5dp"
        android:background="@drawable/background_edit_text_blure_red_80_rounded"
        android:padding="5dp"
        android:src="@drawable/ic_remove"
        android:tint="@color/white"
        app:layout_constraintBottom_toBottomOf="@id/player_field"
        app:layout_constraintEnd_toEndOf="@id/player_field"
        app:layout_constraintTop_toTopOf="@id/player_field" />

</androidx.constraintlayout.widget.ConstraintLayout>